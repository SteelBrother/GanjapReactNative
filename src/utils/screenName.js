  const homeStack = {
    tab: "HomeTab",
    tabStack:"HomeTab"
  };
  const plantsStack = {
    tab: "PlantsTab",
    plantas:"Plantas",
    addPlant: "AddPlant",
  };
  const environmentStack = {
    tab: "EnvironmentTab",
    environment:"Environment"
  };
  const galleryStack = {
    tab: "GalleryTab",
    tabStack:"Gallery"
  };
  const feedStack = {
    tab: "FeedTab",
    tabStack:"Feed"
  };
  const configStack = {
    tab: "ConfigTab",
    tabStack:"Config"
  };

  const expStack = {
    tab: "ExpTab", // O el nombre que corresponda en tu configuración
    // Otras propiedades si las necesitas
  };
  const AgeValidationStack = {
    tab: "AgeTab", // O el nombre que corresponda en tu configuración
    // Otras propiedades si las necesitas
  };
  const Auth = {
    register: "Register", // O el nombre que corresponda en tu configuración
    // Otras propiedades si las necesitas
  };
  export const screen = {
    Home: homeStack,
    Plants: plantsStack,
    Environment: environmentStack,
    Gallery: galleryStack,
    Feed: feedStack,
    Config: configStack,
    ExpStack:expStack,
    AgeVal:AgeValidationStack,
    Auth:Auth
  };
  